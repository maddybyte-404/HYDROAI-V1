import { PageHero } from "@/components/ui/page-hero"; import { SectionHeading } from "@/components/ui/section-heading"; import { StatusBadge } from "@/components/ui/status-badge"; import { technologies } from "@/data/technology";
export const metadata={title:"Technology"};
export default function TechnologyPage(){return <main><PageHero eyebrow="Technology" title="Measure the system before asking it to predict." copy="Hydro AI is developing a connected sensing, edge and decision-support architecture for controlled-environment agriculture."/><section className="section"><div className="grid gap-5">{technologies.map(item=><article className="card grid gap-5 p-6 lg:grid-cols-[.7fr_1fr_1fr_1fr]" key={item.id}><div><div className="flex justify-between gap-3"><h2 className="text-2xl font-bold">{item.name}</h2><StatusBadge status={item.status}/></div><div className="mt-5 flex flex-wrap gap-2">{item.parameters.map(x=><span className="rounded bg-white/8 px-2 py-1 text-xs" key={x}>{x}</span>)}</div></div><div><p className="eyebrow">What it is</p><p className="muted mt-2 text-sm leading-relaxed">{item.what}</p></div><div><p className="eyebrow">Why it matters</p><p className="muted mt-2 text-sm leading-relaxed">{item.why}</p></div><div><p className="eyebrow">Hydro AI role</p><p className="muted mt-2 text-sm leading-relaxed">{item.role}</p></div></article>)}</div></section><section className="section pt-0"><SectionHeading eyebrow="Sensor fusion" title="Multiple imperfect signals + context + time-series data." copy="Hydro AI treats the farm as a connected system rather than a collection of isolated sensor readings."/></section></main>}
