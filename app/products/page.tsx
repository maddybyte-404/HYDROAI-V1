import { PageHero } from "@/components/ui/page-hero"; import { ProductCard } from "@/components/cards/product-card"; import { products } from "@/data/products";
export const metadata={title:"Products"};
export default function ProductsPage(){const features=["Standalone operation","Sensor monitoring","Automation","Data logging","Camera","Edge intelligence","Anomaly detection","Analytics","Prediction","Decision support"];return <main><PageHero eyebrow="Products" title="Modular layers for measured hydroponics." copy="Initial target pricing is shown for product-development discussions. Product capability and final configuration remain subject to validation."/><section className="section"><div className="grid gap-5 lg:grid-cols-3">{products.map(x=><ProductCard key={x.id} product={x}/>)}</div><div className="mt-5 card overflow-x-auto p-6"><h2 className="text-xl font-bold">Product comparison</h2><table className="mt-5 w-full min-w-[650px] text-left text-sm"><thead className="border-b border-white/10 text-white/55"><tr><th className="pb-3">Feature</th>{products.map(x=><th key={x.id}>{x.name}</th>)}</tr></thead><tbody>{features.map((feature,i)=><tr className="border-b border-white/5" key={feature}><td className="py-3">{feature}</td>{products.map((p,j)=><td key={p.id}>{i<(j===0?3:j===1?6:9)?"Planned":"—"}</td>)}</tr>)}</tbody></table></div><p className="muted mt-4 text-sm">Connected — ₹299/month target · AI Grow — ₹699/month target. Target recurring service pricing; final pricing subject to product validation and configuration.</p></section></main>}
