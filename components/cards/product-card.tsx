import { Check } from "lucide-react"; import type { Product } from "@/data/products"; import { StatusBadge } from "@/components/ui/status-badge";
export function ProductCard({product}:{product:Product}) { return <article className="card flex flex-col p-6"><div className="flex items-center justify-between"><p className="eyebrow">{product.category}</p><StatusBadge status={product.status}/></div><h3 className="mt-4 text-3xl font-bold">{product.name}</h3><p className="muted mt-3 leading-relaxed">{product.description}</p><ul className="mt-6 flex-1 space-y-3 text-sm">{product.capabilities.map(item=><li className="flex gap-2" key={item}><Check size={16} className="mt-0.5 shrink-0 text-hydro"/>{item}</li>)}</ul><p className="mt-8 text-2xl font-bold">{product.price}</p><p className="muted mt-1 text-xs">Initial target pricing; subject to product validation and configuration.</p></article>; }
