import { PageHero } from "@/components/ui/page-hero"; import { SectionHeading } from "@/components/ui/section-heading"; import { ConfigurationTool } from "@/components/interactive/configuration-tool"; import { solutions } from "@/data/solutions";
export const metadata={title:"Solutions"};
export default function SolutionsPage(){return <main><PageHero eyebrow="Solutions" title="Different starting points. One evidence-led approach." copy="Hydro AI is designed for growers, researchers, institutions and integration partners—without pretending every use case has identical needs."/><section className="section"><div className="grid gap-5 md:grid-cols-2">{solutions.map(x=><article className="card p-6" key={x.name}><p className="eyebrow">{x.audience}</p><h2 className="mt-3 text-2xl font-bold">{x.name}</h2><p className="muted mt-4 leading-relaxed">{x.description}</p><p className="mt-6 text-sm font-bold">Recommended starting point <span className="text-aqua">— {x.starting}</span></p></article>)}</div></section><ConfigurationTool/></main>}
